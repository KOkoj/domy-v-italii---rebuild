{
  "buildCommand": "cd backend && npm install && npx prisma generate && npx tsc",
  "functions": {
    "backend/dist/api/index.js": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/dist/api/index"
    }
  ]
}
